import React from "react";

import { NavbarContainer } from "./NavbarElements";

const Navbar = () => {
  return <NavbarContainer>Navbar works!</NavbarContainer>;
};

export default Navbar;

// Nav links spread evenly across horizontally, shopping cart right corner listing number of items
// and total amount of order, onClick displays drop down menu with product details and prices
