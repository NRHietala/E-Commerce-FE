import { createSlice } from "@reduxjs/toolkit";

export const weatherSlice = createSlice({
  name: "products",
  initialState: [],
  reducers: {},
  extraReducers: {},
});

// export extraReducer actions below

// export const { } = weatherSlice.actions;

export default weatherSlice.reducer;
