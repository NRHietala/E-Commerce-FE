import React from "react";

import { HeaderContainer } from "./HeaderElements";

const Header = () => {
  return <HeaderContainer>Header works!</HeaderContainer>;
};

export default Header;

// Will contain logo | store name | and login/signup buttons respectively
